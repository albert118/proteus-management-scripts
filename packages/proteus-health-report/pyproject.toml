[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_project_name"
version = "0.1.0"
description = "A description of my existing project"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [] # Add external pip packages here
